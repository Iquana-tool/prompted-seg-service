# Prompted Segmentation Service Environment Variables
# Copy this file to .env for local development

# Include backend URL that will call this service
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000

# Service Port
PORT=8001

# Logging Configuration (use absolute paths or let app use defaults)
# LOG_DIR=/app/logs

# Note: When running in Docker, paths should be absolute (starting with /)
# The docker-compose.yml already sets the correct paths for LOG_DIR and HYDRA_CONFIGS_DIR

